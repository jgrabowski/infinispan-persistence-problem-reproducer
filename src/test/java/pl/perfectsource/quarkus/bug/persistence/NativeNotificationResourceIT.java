package pl.perfectsource.quarkus.bug.persistence;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeNotificationResourceIT extends NotificationResourceTest {

    // Execute the same tests but in native mode.
}